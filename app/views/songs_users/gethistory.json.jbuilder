json.songs_users @songs_users do |s|

  # json.song_id s.song_id
  # json.song_title s.song_title
  # json.preview s.preview
  # json.album s.album
  # json.img s.img
  # json.artist_name s.artist_name
  # json.artist_id s.artist_id
  json.songs songs_users.songs

end
